# 版本记录

## 0.3.0

- 保留标准小体积 Skill 结构；增加 Windows 首次调用自动安装和可双击的 Install.cmd / Run.cmd，不打包 Python、PyTorch 或模型。
- 独立 Python 3.11 环境、CPU / CUDA 推理依赖、固定源码版本与约 7.1 GB 模型自动下载；续传、哈希校验、缓存复用和安装日志。
- 安装后实际生成不同于参考的新句并合成 MP4，通过后才登记成功；持久引擎与影片工程分开。
- 修复 Windows 中文目录下的文本规范化库读取，以及权重映射读取崩溃；锁定所需音频依赖，阻止重复模型任务抢占内存。
- 增加场景计划导入、双参考提取、Whisper 转录和后端自动配置入口。

## 0.2.1

- 固定仓库文本使用 LF 换行，避免 Windows Git 自动换行改变从 checkout 构建的 ZIP 哈希。制作和配音行为与 0.2.0 相同。

## 0.2.0

- 发行包支持从 Git checkout 构建，跳过 `.git` 元数据，继续检查其他文件；加入 Git 元数据不影响 ZIP 哈希的回归测试。
- 增加贡献、问题报告与源码发布说明。
- 根据第二场真实双人对话的制作，补充片源音画末端不一致时的时间轴处理方法。
- 真实换片测试与试听反馈见 RELEASE_NOTES；自动检查与表演验收分别记录。

## 0.1.0

- 首个本地可安装版本：完整场景查找、角色账本、改稿、原场参考、整句生成与成片检查流程。
- 可运行项目 CLI、可选 IndexTTS 2.5 适配器、词时间字幕提案、内容寻址素材与版本、混音和最终 MP4 检查。
- 合成示例、Windows 本地测试与 Windows／Ubuntu CI 定义。
