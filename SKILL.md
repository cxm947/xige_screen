---
name: scene-redub
description: "把视频链接和主题做成影视二创配音短片：找准完整场景，按原戏人物动机和换人节奏改写台词，用同场表演参考整句重录，逐句核对角色、时长、字幕和成片。适用于保留原声语气但更换台词、电影片段讽刺改编、反复打磨剧本与配音，也可继续已有二创工程。"
---

# Scene Redub

交付能看懂、能听清、接得上原戏的短片。脚本、声音和验收证据一起保存，下一轮只修出问题的句子。

## 接住用户当前阶段

已有工程先运行 `status`，读最近的反馈、剧本和选片记录。不要重新问答过的问题、重做已认可的部分。“梗没懂”回到剧本；“像他但不像这一段”改表演参考；“是另一个人的声音”先查角色账本，不能靠换随机种子碰运气。

新工程只需视频链接和主题。能从上下文确定的片段与风格直接采用，只问无法推断且影响下一步的缺项。片段不明确时先查看链接，给出具体候选并继续查片源。普通检查、改稿、试配和本地导出不额外索要许可。

`<skill>` 是本目录；`<project>` 是独立制作目录。中间文件放项目 `work/`，发行包不放项目媒体。

```text
python <skill>/scripts/redub.py doctor
python <skill>/scripts/redub.py init <project> --url "视频链接" --topic "用户主题"
python <skill>/scripts/redub.py status <project>
```

结构与命令见 [project-contract.md](references/project-contract.md)。没有模型也先完成找片、角色账本和剧本；预检不自动下载大模型。

## 制作顺序

### 1. 找到完整目标场景

读 [source-and-cast.md](references/source-and-cast.md)。确认作品和原句，用片名、角色、标志性台词搜索。比较开头、结尾、对话连续性、画质和水印；长不等于完整，完整电影也不等于用户要的场景。

用搜索／浏览器查看候选；`inspect-url` 可取元数据，选定后用 `fetch` 或导入文件。登录失败、下架或工具不支持时保存证据、换可用来源，必要时才请求可访问的文件。下载成功不代表完整性核实。

记录首句、末句、相邻镜头和保留区间。台词使用**原片时间**；最终时间由保留区间映射，不手抄固定偏移。

### 2. 锁准谁在什么时候说话

建 `speakers` 与 `turns`。每次实际换人就是一条，即使 ASR 将五次接话合成了一段。画面中的人可能正在听，反应镜头不能单独证明说话人。

记下角色证据、原话、新话、开口／收口、戏剧功能、表演参考和要保留的笑声／碰杯／名句。重叠人声需要独立分轨；当前单轨合成器会阻止重叠，不悄悄覆盖另一人。

### 3. 先写成一场戏，再打磨包袱

读 [writing-room.md](references/writing-room.md)。按原戏的请求、反问、试探、谈条件、突发事件和收尾移植冲突。角色为了得到、拒绝、诱导、讨价还价而说话，素材不能挤掉因果线。

资料分事实、公开指控、传闻、虚构经历，允许传闻和夸张，措辞保留证据身份。虚构例子不能伪装成实测，“有人指控”不能改成已经证实。

每个包袱检查：**发生什么、谁得利／吃亏、最后一句刺中什么**。需要靠解释才能懂的梗先改稿。用具体动作、钱和后果托住内行术语。最后十句必须能复述出结尾因果。

给一份可配音的主稿和必要的局部备选，记下用户具体接受／否定了什么。文字未稳时只做小样。当前授权足够时可记录明确标为 agent 的自检继续，不冒充用户认可。

### 4. 用原场表演做小样

读 [performance.md](references/performance.md)，按需读 [backends.md](references/backends.md)。音色相像、词说清楚、语气气口像原片分别验收。

默认整句／自然意群重录。用同场、同角色、对应这句戏剧动作的表演参考；极短原句不足以支撑音色时，另加同场同人的干净音色参考，不混入另一角色。

选代表性难句，做原片→方案 1→方案 2 对照。字幕、词、播放音量一致，标注开始时间。每次只改变一个主要因素，记录选择理由。用户选好后成为基准，不继续无目标换模型。

本包有可选 IndexTTS 2.5 本地适配器，也可导入其他工具的完整新台词录音；按本项目小样选型，不保证任意演员／语言完美复刻。

### 5. 批量生成，逐句验收

先 `validate --stage generate`。配音收据绑定台词、角色、时窗、两种参考的内容哈希、模型代码／权重版本和参数。改词、换人、换参考后旧 take 失效；未改句继续复用。

语速自然优先，不把一秒的话拉满三秒停顿。超时先减赘词、按自然句拆分，或适度使用模型内时长控制重录；有声尾字超窗不能砍掉。专业词先调文本读音，不拼音频单字。少量有理由的重试仍无改善，就回查参考和句子。

ASR 查漏字与重复，声纹查疑似串角，能量查空音轨；这些均不证明表演好。核对同音词、外文名和吞字误报。无法完成的听觉检查明确记录，不能写“已试听通过”。

### 6. 合成后再验一次

读 [acceptance.md](references/acceptance.md)。保留现场底声与指定原声事件，清除旧台词，避免双层人声。字幕按实际词边界／复核时间切，不能只按字数摊时间。

```text
python <skill>/scripts/redub.py validate <project> --stage render
python <skill>/scripts/redub.py render <project>
python <skill>/scripts/redub.py report <project>
```

合成器按输入内容创建版本目录。最终 MP4 完整解码、发言非空、逐句与预期混音一致、末句未被编码截掉后才写 verified 收据。未审稿小样用 `render --preview`，结构检查仍执行。机器 verified 只代表装配检查，不代表表演获认可。

交付能直接播放的成片、剧本／SRT、改动说明和仍需试听的位置。收到反馈只回退对应阶段，保留选中版本。续做先查状态，不重复启动还在运行的进程。

## 失败后优先查什么

| 现象 | 优先动作 |
|---|---|
| 看不懂／没讽刺感 | 补行为、利益和后果，删猜谜式比喻，再整句重录 |
| A 的台词像 B | 查换人、参考文件和 take 收据，先修角色 |
| 像演员、语气不对 | 用对应原句或相邻同场的表演参考 |
| 单字接缝／口气断 | 回到完整新句，不继续贴字 |
| 末尾没声音 | 查时窗、实际音频和最终解码时戳 |
| 下载／推理超时 | 查原进程、日志、项目锁、磁盘；不重复提交 |
| 没有可用配音后端 | 先完成剧本、分句、参考清单，配置实际后端或导入录音 |

工程测试与发行边界见 [README.md](README.md)。`scripts/make_demo.py` 仅生成色块和音调，不含影视或真人声音。
