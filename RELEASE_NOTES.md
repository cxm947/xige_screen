# v0.3.0 · Windows 首次自动安装 Skill

此前需要用户自行准备 Python、推理环境和模型；现在下载小体积 Skill ZIP 后，双击 `Install.cmd`，或让本地桌面 agent 首次调用 `Run.cmd`，即可自动安装依赖、下载模型并实际生成语音和测试视频。ZIP 不包含 Python 本体、PyTorch 或模型权重。

- 标准 `SKILL.md` / `agents` / `scripts` / `references` / `assets` 结构；安装与制作逻辑随 Skill 开源。
- 独立 Python 3.11.15、固定依赖和 IndexTTS 2.5 源码版本；约 7.1 GB 模型自动下载、续传与 SHA-256 校验。已验证缓存复用，持久引擎与影片工程分离。
- GPU 资源充足时选择 CUDA，否则自动使用 CPU；处理中文安装目录、Windows 权重读取兼容和重复推理占用内存。
- 增加场景计划导入、双参考提取、模型生成和 Whisper 转录入口。原有整句生成、角色账本、素材哈希、字幕与最终音轨校验继续保留。

## 已完成的验证

- 60 项本地工程测试通过，无跳过；`skill-creator` 格式验证和 Windows PowerShell 入口解析通过。
- 解压 ZIP 后，通过 CMD 启动，PATH 中仅保留 Windows 系统工具；不依赖系统 Python、Git 或 Conda。首次 `Run.cmd doctor` 自动完成安装、真实语音生成、12 秒 MP4 合成，再继续原 doctor 命令，退出码为 0。
- 另一空目录的完整 CPU 安装通过：Python 与全部依赖从头下载，无预装虚拟环境；安装根和解压目录均包含中文及空格。模型使用另处已下载缓存，但逐文件重新核验大小和 SHA-256，最终真实自检与安装均退出 0。
- CPU 与 CUDA 均实际生成了不同于参考的英文新句并通过最终 MP4 完整解码和音轨检查。CUDA 在当前剩余提交额度不足时复测失败，因此安装器检查实际可用资源，自动选择更稳妥的 CPU 路径。
- 独立测试从解压包完成中文参考提取、整句生成、词时间字幕、12 秒 MP4、最终解码与转录。新语音 3.947 秒；原始 WAV 转录在繁简归一后与目标一致，成片转录出现“经/精”同音字差异，未冒充人工试听通过。独立重建响度处理后的 PCM 与最终 AAC 相关系数约 0.999994。
- 独立测试发现隔离 TEMP 会让进程锁分散；改为稳定用户级位置，并用两个实际子进程验证不同 TEMP 下也会阻止重复推理。
- 安装后通过 Codex app-server 的 `skills/list` 读取到启用的 `scene-redub`，显示名为 `xige_screen · 影视二创配音`。未用 UI 自动化冒充 ChatGPT 操作验收。

这是本机 Windows x64 的隔离环境验收，不是覆盖所有 Windows 硬件的裸机测试。首次下载需要可用网络和足够磁盘；CPU 较慢。系统语音自检证明依赖、模型与视频装配能运行，演员语气仍需按原片小样试听。客户端须支持本地 Skill 和程序执行；把 ZIP 发到普通云端聊天不会安装本机引擎。

模型与源码的锁定版本可在包内清单及安装器核对；公开包不含测试音频、影片、私人配置或运行环境。完整用法见 [Windows 指南](references/windows.md)。

---

# v0.2.1 · 2026-09-21

在 0.2.0 发布整理上补充 `.gitattributes`，统一 LF 换行；防止 Windows Git 自动换行使源码 checkout 与 ZIP 的文本字节不一致。配音、混音与媒体检查逻辑未改动。以下换片结果由相同的 0.2 系列制作核心取得。

面向源码发布的整理版本，增加第二场真实双人对话测试、贡献与问题反馈说明，修复从 Git checkout 打包时把 `.git` 视为发行文件的问题。

- Windows、Python 3.12.4、FFmpeg 7.1：50 项本地工程测试通过，无跳过；包含 Git 元数据不改变可复现 ZIP 的新增测试。
- 换片测试使用新角色、新音色及对应原句表演参考，通过包内适配器生成九句新词，再用包内合成器输出 38.84 秒双人场景。十次发言通过最终解码与混音一致性检查。
- 具体方法、修正和边界见 [第二场测试记录](references/cross-scene-test.md)。本轮换片已获委托方试听认可并授权发布；这是当前样片的反馈，不代表任意演员均能达到相同效果。
- Skill 格式检查通过。远端 CI 是否完成应以实际 GitHub Actions 记录为准；本地测试不冒充跨平台验证。

正式发布入口：[v0.2.1](https://github.com/cxm947/xige_screen/releases/tag/v0.2.1)。

---

# v0.1.0 · 2026-09-21（历史本地版本）

首个可安装版本，包含制作工作流、项目契约、CLI、可选 IndexTTS 2.5 适配器、字幕对齐提案器、合成示例、测试、CI 配置与确定性打包工具。没有上传到远程仓库。

## 本次实际验证

- Windows、Python 3.12.4、FFmpeg 7.1：49 项自动化测试通过，无跳过。包括项目／角色／引用校验、输入变化失效、不可变素材、锁、混音、真实 MP4 解码、字幕边界、成片尾部与发行包检查。
- Skill frontmatter 验证通过；全部发行文本以 UTF-8 读取，界面元数据已核查。
- 独立 agent 从文档开始做了 19 步实际操作：初始化、首次双角色合成、改 A、被陈旧审稿／take 拦截、导入完整新 A、复用 B、导出第二版、缓存复用。独立频谱和截图检查通过。仅使用色块和音调，无联网、真实媒体或 GPU 模型。
- 同一真实制作项目的私有素材用于适配器集成：IndexTTS 2.5 读取双参考并整句生成；首次超窗 0.416 秒被合成器拦截，调整生成时长后输出 5.851 秒，成功合成 5.900 秒片段，最终解码约 5.909 秒；再次调用核验并复用缓存，没有重新加载生成模型。
- 原制作项目的十段改词字幕使用本包对齐函数生成词时间提案，再检查字幕画面。源片、参考音、实际生成声音和工程绝对路径均未进入发行包。

以上真实素材测试验证接线、缓存和合成流程，不证明所有演员、语言、主题都能达到同等表演效果。49 项自动测试中的合成音调不冒充真实台词识别或演员试听。

## 独立测试发现并处理的点

- 未改 B 的原始素材与归一化前混音完全复用；全片 loudnorm 仍可能改变最终 B 的电平。本次例子变化约 +1.26 dB，文档已明确这一边界。
- 补充 revise_demo.py，提供开箱可跑的“改 A、保留 B”续做演练。
- 初始界面 YAML 曾受 Windows 默认编码影响，发行前改为 UTF-8，并加入打包编码检查。

## 没有声称完成的验证

Linux／macOS、本地 GPU 的其他硬件组合、各平台实时下载、云端配音 API、任意演员音色、自动人声分离、完整真人听觉验收都不在上述测试结论内。仓库提供 Windows／Ubuntu CI 配置，但远端 CI 未执行。

## 依赖与复现

核心标准库 Python 3.10+；本次核心实测 3.12.4，模型环境另用 3.11。媒体依赖允许版本范围，不是全平台锁定环境。运行 doctor 记录实际版本；模型适配器记录代码、权重内容哈希与运行时版本。不同设备或模型推理实现不保证逐样本确定性，所以 take ID 同时包含真实音频哈希。

## 发行检查

package_release.py 以允许列表打包，拒绝媒体、权重、大文件、非 UTF-8、符号链接、常见私人目录／凭据模式及未声明文件，生成逐文件 SHA-256 清单。ZIP 使用固定成员时间戳，可复现构建；测试包括解压后重新打包的相同哈希。模式扫描不等于穷尽式秘密检测。
